<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jeonsaeyukjun.jeonsaeyukjunbe.contract.mapper.FileMapper">

    <!-- Contract 저장 (reportId가 null이면 NULL 삽입) -->
    <insert id="insertContract">
        INSERT INTO Contract (contract_name, contract_url, user_id, report_id)
        VALUES (
        #{contractName},
        #{contractUrl},
        #{userId},
        <if test="reportId != null">
            #{reportId}
        </if>
        <if test="reportId == null">
            NULL
        </if>
        )
    </insert>

    <!-- Contract 이름으로 조회 -->
    <select id="findByContractName" resultType="FileDto">
        SELECT * FROM Contract WHERE contract_name = #{contractName}
    </select>

</mapper>

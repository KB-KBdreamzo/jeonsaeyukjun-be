<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeonsaeyukjun.jeonsaeyukjunbe.map.mapper.AccidentMapper">

    <select id="selectAccidents" resultType="AccidentDto">
        SELECT region_name, incident_count, incident_amount, incident_rate, latitude, longitude
        FROM accidents
        WHERE month = '7월';
    </select>

</mapper>
